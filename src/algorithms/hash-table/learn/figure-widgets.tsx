// Мапа «фігура README → живий віджет» для навчальної вкладки хеш-таблиці.
// Поки без живих фігур (контент текстовий) — повертаємо null; місце для майбутніх
// віджетів (міні-конвеєр хешування, таблиця з колізією, датчик α).

import type { ReactNode } from "react"

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function figureForSrc(_src: string | undefined, _alt: string | undefined): ReactNode {
  return null
}
