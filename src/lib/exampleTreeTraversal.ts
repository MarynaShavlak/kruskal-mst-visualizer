// Канонічні приклади дерев — ЄДИНЕ джерело правди для тестів lib/, пресетів редактора
// й живих навчальних віджетів (щоб послідовності обходів НЕ дрейфували між екранами).
// Головний приклад — дерево з конспекту edu.goit: корінь 1, діти 2 і 3, у 2 діти 4 і 5.
// Дерева задано РІВНЕВОЮ серіалізацією (BFS): значення зверху-вниз, зліва-направо,
// `null` — порожня дитина. Без React.

/**
 * Головне навчальне дерево (з конспекту):
 *         1
 *        / \
 *       2   3
 *      / \
 *     4   5
 * Обходи: preorder 1,2,4,5,3 · inorder 4,2,5,1,3 · postorder 4,5,2,3,1.
 */
export const BT_INTRO_LEVELS: readonly (number | null)[] = [1, 2, 3, 4, 5]

/** Відомі послідовності обходів головного прикладу (звіряють тести й підписи). */
export const BT_INTRO_ORDERS = {
  preorder: [1, 2, 4, 5, 3],
  inorder: [4, 2, 5, 1, 3],
  postorder: [4, 5, 2, 3, 1],
} as const

/**
 * Відомі підсумки головного прикладу. Кадрів у trace завжди 4·n+3 (init + done +
 * enter/visit/leave на кожен вузол + база на кожну порожню дитину, яких n+1):
 * 4·5+3 = 23, однаково для всіх трьох порядків.
 */
export const BT_INTRO_STATS = {
  nodes: 5,
  leaves: 3,
  height: 2,
  frames: 23,
} as const

/**
 * Дерево двійкового ПОШУКУ (BST): ліворуч менше, праворуч більше. Родзинка обходів —
 * ЦЕНТРОВИЙ (inorder) обхід BST дає ВІДСОРТОВАНУ послідовність. Місток до двійкового
 * пошуку.
 *          8
 *        /   \
 *       4     12
 *      / \   /  \
 *     2   6 10   14
 */
export const BT_BST_LEVELS: readonly (number | null)[] = [8, 4, 12, 2, 6, 10, 14]

/** Центровий обхід BST — відсортовано (перевіряють тести й навчальний віджет). */
export const BT_BST_INORDER: readonly number[] = [2, 4, 6, 8, 10, 12, 14]

/**
 * Вироджене дерево — «ланцюг» лише лівих дітей (1→2→3→4→5). Фактично зв'язаний список:
 * висота = n−1, обхід лінійний O(n). Наочно, чому загальне дерево повільніше за
 * збалансоване.
 */
export const BT_CHAIN_LEVELS: readonly (number | null)[] = [
  1, 2, null, 3, null, 4, null, 5,
]

/**
 * Повне (збалансоване) дерево з 7 вузлів — симетричне, висота 2.
 *          1
 *        /   \
 *       2     3
 *      / \   / \
 *     4   5 6   7
 */
export const BT_FULL_LEVELS: readonly (number | null)[] = [1, 2, 3, 4, 5, 6, 7]
