// Канонічні приклади сортування злиттям (еталон коректності з README та
// Python-репозиторію algo-merge-sort). Ті самі масиви розбирає навчальна вкладка;
// їхні відомі підсумки (порівняння/додавання/злиття/глибина) звіряють тести. Без React.

/**
 * Основний навчальний масив `[8, 4, 6, 2, 7, 1, 5, 3]` — 8 елементів (степінь
 * двійки), тож дерево поділу ІДЕАЛЬНО ЗБАЛАНСОВАНЕ (глибина 3). Низхідна й
 * вихідна версії дають однакові числа: 17 порівнянь, 24 додавання, 7 злить.
 * Сортується у `[1..8]`. На ньому README веде весь покроковий розбір.
 */
export const MERGE_INTRO: readonly number[] = [8, 4, 6, 2, 7, 1, 5, 3]
/** Відсортований результат головного прикладу. */
export const MERGE_INTRO_SORTED: readonly number[] = [1, 2, 3, 4, 5, 6, 7, 8]

/**
 * Масив із конспекту `[5, 3, 8, 4, 2]` — на ньому README будує панелі «код ↔ дані»
 * й відтворює дерево/таблицю з конспекту. НЕ степінь двійки, тож низхідна
 * (8 порівнянь) і вихідна (6) трохи різняться. Сортується у `[2, 3, 4, 5, 8]`.
 */
export const MERGE_CONSPECT: readonly number[] = [5, 3, 8, 4, 2]

/**
 * Уже відсортований `[1..8]` — merge sort усе одно робить ВСІ поділи й злиття
 * (базова версія не адаптивна): 12 порівнянь, 24 додавання, 7 злить, глибина 3.
 */
export const MERGE_SORTED: readonly number[] = [1, 2, 3, 4, 5, 6, 7, 8]

/**
 * Зворотно відсортований `[8..1]` — дерево так само збалансоване, складність так
 * само O(n·log n): НЕМАЄ деградації (на відміну від quicksort). 12/24/7/глибина 3.
 */
export const MERGE_REVERSED: readonly number[] = [8, 7, 6, 5, 4, 3, 2, 1]

/**
 * Масив із дублікатами (база `[3, 1, 3, 2, 1, 3]`) — демонструє СТАБІЛЬНІСТЬ:
 * рівні ключі зберігають початковий порядок завдяки нестрогому `<=`.
 */
export const MERGE_DUPLICATES: readonly number[] = [3, 1, 3, 2, 1, 3]

/** Дві відсортовані половини для окремої демонстрації злиття (з README/прикладів). */
export const MERGE_DEMO_LEFT: readonly number[] = [3, 5]
export const MERGE_DEMO_RIGHT: readonly number[] = [2, 4, 8]

/** Відомі підсумки головного прикладу (низхідна версія, з README). */
export const MERGE_INTRO_STATS = {
  comparisons: 17,
  appends: 24,
  merges: 7,
  depth: 3,
} as const
