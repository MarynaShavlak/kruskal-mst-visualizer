// Канонічні приклади порозрядного сортування (еталон коректності з README та
// Python-репозиторію algo-radix-sort). Ті самі масиви розбирає навчальна вкладка;
// їхні відомі підсумки (проходи/розкладання) звіряють тести. Без React.

/**
 * Основний навчальний масив `[3, 89, 67, 254, 9, 21, 185, 4, 62]` (9 чисел,
 * 1–3 розряди). На ньому README веде весь покроковий розбір. Максимум — 254
 * (три цифри) → 3 розрядні проходи (одиниці / десятки / сотні), 27 розкладань,
 * 0 порівнянь елементів. Сортується у `[3, 4, 9, 21, 62, 67, 89, 185, 254]`.
 */
export const RADIX_INTRO: readonly number[] = [3, 89, 67, 254, 9, 21, 185, 4, 62]
/** Відсортований результат головного прикладу. */
export const RADIX_INTRO_SORTED: readonly number[] = [3, 4, 9, 21, 62, 67, 89, 185, 254]

/**
 * Рівнорозрядний масив — усі двозначні, чисті 2 проходи (добре видно роботу за
 * розрядами без «нульового старшого розряду»). 16 розкладань.
 */
export const RADIX_EQUAL: readonly number[] = [42, 17, 93, 28, 55, 31, 86, 64]

/**
 * Масив із дублікатами (база `[52, 12, 52, 31, 12, 41]`) — демонструє СТАБІЛЬНІСТЬ:
 * рівні ключі (два 52, два 12) зберігають взаємний порядок між проходами. 2 проходи.
 * Сортується у `[12, 12, 31, 41, 52, 52]`.
 */
export const RADIX_DUPLICATES: readonly number[] = [52, 12, 52, 31, 12, 41]

/**
 * «Межовий» приклад `[4, 7, 1000000, 23, 9]`: лише 5 чисел, але max = 10⁶ →
 * аж 7 розрядних проходів (d ≫ log n). Показує ОБМЕЖЕННЯ: коли чисел мало, а самі
 * вони величезні, перевага radix зникає. 35 розкладань.
 */
export const RADIX_BIG: readonly number[] = [4, 7, 1000000, 23, 9]

/** Відомі підсумки головного прикладу (з README). */
export const RADIX_INTRO_STATS = {
  passes: 3,
  distributions: 27,
  comparisons: 0,
} as const
