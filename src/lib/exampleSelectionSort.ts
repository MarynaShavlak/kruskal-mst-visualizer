// Канонічні приклади сортування прямим вибором (еталон коректності з README та
// Python-репозиторію algo-selection-sort). Ті самі масиви розбирає навчальна
// вкладка; їхні відомі підсумки (порівняння/обміни/записи) звіряють тести.
// Без імпортів React — суто дані.

/**
 * Основний навчальний масив `[5, 3, 8, 4, 2, 7]`. Сортується у `[2, 3, 4, 5, 7, 8]`
 * за 15 порівнянь і лише 4 справжні обміни (6 проходів; на проходах 1 і 5 мінімум
 * уже стоїть на місці — самообмін). На ньому README веде весь покроковий розбір.
 */
export const SELECTION_INTRO: readonly number[] = [5, 3, 8, 4, 2, 7]
/** Відсортований результат головного прикладу. */
export const SELECTION_INTRO_SORTED: readonly number[] = [2, 3, 4, 5, 7, 8]

/**
 * Масив із конспекту `[5, 3, 8, 4, 2]` — на ньому README будує панелі
 * «код ↔ масив» (трасування рядок-у-рядок): 10 порівнянь, 3 обміни.
 */
export const SELECTION_CONSPECT: readonly number[] = [5, 3, 8, 4, 2]

/**
 * Найкращий випадок — уже відсортований масив `[1..6]`. Кожен мінімум уже стоїть
 * на місці, тож 0 справжніх обмінів — але всі 15 порівнянь однаково зроблено
 * (метод НЕ адаптивний).
 */
export const SELECTION_BEST: readonly number[] = [1, 2, 3, 4, 5, 6]

/**
 * Гірший випадок (за обмінами) — зворотно відсортований масив `[6..1]`. Та сама
 * кількість порівнянь (15), але обмінів більше — 3 (хоч і все одно ≤ n−1).
 */
export const SELECTION_WORST: readonly number[] = [6, 5, 4, 3, 2, 1]

/** Відомі підсумки стандартної версії на головному прикладі (з README). */
export const SELECTION_INTRO_STATS = {
  comparisons: 15,
  swaps: 4,
  passes: 6,
} as const
