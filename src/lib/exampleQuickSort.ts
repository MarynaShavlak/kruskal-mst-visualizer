// Канонічні приклади швидкого сортування (еталон коректності з README та
// Python-репозиторію algo-quick-sort). Ті самі масиви розбирає навчальна вкладка;
// їхні відомі підсумки (порівняння/виклики/глибина) звіряють тести. Без React.

/**
 * Основний навчальний масив `[3, 5, 2, 4, 6, 1, 7]`. Опорний-середина щоразу
 * влучає в медіану → ЗБАЛАНСОВАНЕ дерево: 13 порівнянь, 7 викликів, глибина 3.
 * Сортується у `[1, 2, 3, 4, 5, 6, 7]`. На ньому README веде весь розбір.
 */
export const QUICK_INTRO: readonly number[] = [3, 5, 2, 4, 6, 1, 7]
/** Відсортований результат головного прикладу. */
export const QUICK_INTRO_SORTED: readonly number[] = [1, 2, 3, 4, 5, 6, 7]

/**
 * Масив із конспекту `[5, 3, 8, 4, 2]` — на ньому README будує панелі
 * «код ↔ дані» та псевдографічне дерево: 11 порівнянь, 7 викликів, глибина 4.
 */
export const QUICK_CONSPECT: readonly number[] = [5, 3, 8, 4, 2]

/**
 * Уже відсортований `[1..7]` — той самий вхід для контрасту стратегій опорного:
 * опорний-середина (медіана) дає збалансоване дерево (13/7/глибина 3), а
 * опорний-перший — вироджене (ланцюг): 27 порівнянь, 13 викликів, глибина 7.
 */
export const QUICK_SORTED: readonly number[] = [1, 2, 3, 4, 5, 6, 7]

/**
 * Багато дублікатів `[4, 2, 4, 4, 1, 4, 3]` — корінь-опорний 4 збирає всі чотири
 * четвірки в `middle` одразу; рекурсія працює лише з 3 меншими елементами
 * (7 викликів, глибина 4). Демонструє силу тристороннього поділу.
 */
export const QUICK_DUPLICATES: readonly number[] = [4, 2, 4, 4, 1, 4, 3]

/** Відомі підсумки головного прикладу (опорний-середина, з README). */
export const QUICK_INTRO_STATS = {
  comparisons: 13,
  calls: 7,
  depth: 3,
} as const
