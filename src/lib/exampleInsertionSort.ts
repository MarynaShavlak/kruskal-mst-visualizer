// Канонічні приклади сортування вставками (еталон коректності з README та
// Python-репозиторію algo-insertion-sort). Ті самі масиви розбирає навчальна
// вкладка; їхні відомі підсумки (порівняння/зсуви/вставки) звіряють тести.
// Без імпортів React — суто дані.

/**
 * Основний навчальний масив `[5, 2, 4, 6, 1, 3]` (класичний приклад із CLRS).
 * Сортується у `[1, 2, 3, 4, 5, 6]` за 12 порівнянь і 9 зсувів (5 вставок).
 * На ньому README веде весь покроковий розбір.
 */
export const INSERTION_INTRO: readonly number[] = [5, 2, 4, 6, 1, 3]
/** Відсортований результат головного прикладу. */
export const INSERTION_INTRO_SORTED: readonly number[] = [1, 2, 3, 4, 5, 6]

/**
 * Масив із конспекту `[5, 3, 8, 4, 2]` — на ньому README будує панелі
 * «код ↔ масив» (трасування рядок-у-рядок).
 */
export const INSERTION_CONSPECT: readonly number[] = [5, 3, 8, 4, 2]

/**
 * Найкращий випадок — уже відсортований масив `[1..6]`. Тіло while жодного разу
 * не виконується: лінійна робить лише 5 порівнянь і 0 зсувів (адаптивність без
 * прапорця). Бінарна тут робить БІЛЬШЕ порівнянь (8), бо двійковий пошук не
 * економить на впорядкованих даних.
 */
export const INSERTION_BEST: readonly number[] = [1, 2, 3, 4, 5, 6]

/**
 * Гірший випадок — зворотно відсортований масив `[6..1]`. Кожен новий key менший
 * за весь префікс: лінійна — 15 порівнянь і 15 зсувів; бінарна скорочує порівняння
 * до 11, але зсувів так само 15.
 */
export const INSERTION_WORST: readonly number[] = [6, 5, 4, 3, 2, 1]

/** Відомі підсумки лінійної версії на головному прикладі (з README). */
export const INSERTION_INTRO_STATS = {
  comparisons: 12,
  shifts: 9,
  insertions: 5,
} as const
