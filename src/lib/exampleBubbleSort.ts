// Канонічні приклади бульбашкового сортування (еталон коректності з README та
// Python-репозиторію algo-bubble-sort). Ті самі масиви розбирає навчальна
// вкладка; їхні відомі підсумки (порівняння/обміни/проходи) звіряють тести.
// Без імпортів React — суто дані.

/**
 * Масив із конспекту `[5, 1, 4, 2, 8, 3]`. Сортується у `[1, 2, 3, 4, 5, 8]`
 * за 15 порівнянь і 7 обмінів (5 проходів). Головний приклад README.
 */
export const BUBBLE_INTRO: readonly number[] = [5, 1, 4, 2, 8, 3]
/** Відсортований результат головного прикладу. */
export const BUBBLE_INTRO_SORTED: readonly number[] = [1, 2, 3, 4, 5, 8]

/**
 * Другий масив із конспекту `[5, 3, 8, 4, 2]` — на ньому README будує панелі
 * «код ↔ масив» (трасування рядок-у-рядок).
 */
export const BUBBLE_CONSPECT: readonly number[] = [5, 3, 8, 4, 2]

/**
 * Найкращий випадок — уже відсортований масив `[1..6]`. Оптимізована версія
 * робить лише один прохід (5 порівнянь, 0 обмінів) і зупиняється достроково.
 */
export const BUBBLE_BEST: readonly number[] = [1, 2, 3, 4, 5, 6]

/**
 * Гірший випадок — зворотно відсортований масив `[6..1]`. Кожне порівняння дає
 * обмін: 15 порівнянь і 15 обмінів (рання зупинка не допомагає).
 */
export const BUBBLE_WORST: readonly number[] = [6, 5, 4, 3, 2, 1]

/** Відомі підсумки наївної версії на головному прикладі (з README). */
export const BUBBLE_INTRO_STATS = {
  comparisons: 15,
  swaps: 7,
  passes: 5,
} as const
