// Канонічні приклади сортування Шелла (еталон коректності з README та
// Python-репозиторію algo-shell-sort). Ті самі масиви розбирає навчальна вкладка;
// їхні відомі підсумки (порівняння/зсуви/фази) звіряють тести. Без React.

/**
 * Основний навчальний масив `[8, 5, 3, 7, 6, 1, 4, 2]` (8 елементів). На ньому
 * README веде весь покроковий розбір. Послідовність Шелла (gap 4, 2, 1):
 * 23 порівняння / 11 зсувів / 3 фази. Сортується у `[1..8]`.
 */
export const SHELL_INTRO: readonly number[] = [8, 5, 3, 7, 6, 1, 4, 2]
/** Відсортований результат головного прикладу. */
export const SHELL_INTRO_SORTED: readonly number[] = [1, 2, 3, 4, 5, 6, 7, 8]

/**
 * Масив із конспекту `[5, 3, 8, 4, 2]` — для панелей «код ↔ масив»
 * (gap 2, 1: 9 порівнянь / 3 зсуви / 2 фази).
 */
export const SHELL_CONSPECT: readonly number[] = [5, 3, 8, 4, 2]

/**
 * Уже відсортований `[1..8]` — демонструє АДАПТИВНІСТЬ: 0 зсувів (лише порівняння,
 * усі умови while хибні одразу). Шелла: 17 порівнянь / 0 зсувів / 3 фази.
 */
export const SHELL_SORTED: readonly number[] = [1, 2, 3, 4, 5, 6, 7, 8]

/**
 * Зворотно відсортований `[8..1]` — БАГАТО зсувів. Шелла: 22 порівняння /
 * 12 зсувів / 3 фази.
 */
export const SHELL_REVERSED: readonly number[] = [8, 7, 6, 5, 4, 3, 2, 1]

/**
 * Масив із дублікатами (база `[3, 2, 4, 2, 4, 3]`) — демонструє НЕстабільність:
 * gap-зсуви перестрибують рівні ключі (мічені 2₂ опиняється перед 2₁).
 */
export const SHELL_DUPLICATES: readonly number[] = [3, 2, 4, 2, 4, 3]

/**
 * Більший масив (24 елементи) — для порівняння послідовностей проміжків:
 * Шелла 146/83, Кнута 105/61, Ciura 100/53 порівнянь/зсувів (Ciura найкраща).
 * Завеликий для плеєра (лише навчальна таблиця).
 */
export const SHELL_BIG: readonly number[] = [
  42, 20, 51, 84, 7, 10, 69, 13, 47, 75, 8, 65,
  28, 5, 12, 56, 54, 9, 31, 12, 71, 55, 8, 73,
]

/** Відомі підсумки головного прикладу (послідовність Шелла, з README). */
export const SHELL_INTRO_STATS = {
  comparisons: 23,
  shifts: 11,
  gapPhases: 3,
} as const
